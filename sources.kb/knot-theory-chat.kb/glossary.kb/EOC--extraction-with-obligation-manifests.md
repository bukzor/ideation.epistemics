# end-of-context extraction with obligation manifests

`chat.md:1373`-ff (msg 058), named pattern: serialize
claims-plus-statuses (not conclusions), address each artifact to a
context equipped with a verifier — better default than summarization,
which strips epistemic status. The `../../../prompts/` are its
instance.
