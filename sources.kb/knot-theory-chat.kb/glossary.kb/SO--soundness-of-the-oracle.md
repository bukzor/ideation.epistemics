# SO — soundness of the oracle

`chat.md:659` (msg 034): the central 2MP problem — the LLM generates
candidate design moves but is unsound toward sycophantic collapse;
agreement can be produced by pressure rather than merit.
