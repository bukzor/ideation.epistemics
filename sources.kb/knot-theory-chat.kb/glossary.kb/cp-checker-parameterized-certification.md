# CP — checker-parameterized certification

`chat.md:889` (msg 050): certified(checker) — certification is
parameterized by which checker vouches, with per-claim trust-base
accounting. Adopted in ACS's assumed status set.
