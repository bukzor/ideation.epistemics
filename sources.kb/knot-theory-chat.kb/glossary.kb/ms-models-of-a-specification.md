# MS — models of a specification

`chat.md:162` (superseded branch 009/010; ← corrects SS): the right
"something" is implementations of a signature — a specification listing
the knot operations, with representations as its models.
