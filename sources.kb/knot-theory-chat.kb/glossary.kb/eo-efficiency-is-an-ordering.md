# EO — efficiency is an ordering

`chat.md:243` (msg 012): efficiency slices need a cost model to
become predicates. **Retracted as blocking** — the operator's
cost-model-existence argument won on merits (BD, :366). Survives as
"efficiency slices are relative to a declared cost model".
