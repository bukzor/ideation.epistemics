# NS — no slicing

`chat.md:241` (msg 012; ← PC): "slice the space with constraints" is
malformed because the ambient space is a proper class. **Retracted** by
the assistant; the badgering audit (BD, :366) upheld the retraction as
merit-won.
