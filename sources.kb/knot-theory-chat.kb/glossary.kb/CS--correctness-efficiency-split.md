# CS — correctness/efficiency split

`chat.md:374` (msg 018): refined position on the cost-model fight —
correctness slices are formal, efficiency slices are empirical; keep
the two disciplines separate. Collides with CS2 (:548).
