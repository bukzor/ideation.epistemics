# AU — ambient universe

`chat.md:242` (msg 012): fix a formal language; "all representations
including invalid ones" becomes the set of terms of that language, and
validity becomes a predicate. Later identified as a precondition of TL
(:720).
