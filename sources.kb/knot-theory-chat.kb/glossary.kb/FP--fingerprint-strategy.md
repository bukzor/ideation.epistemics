# FP — fingerprint strategy

`chat.md:554` (msg 026): don't represent all knots injectively;
discriminate within a fixed finite table via cheap invariant
fingerprints. Where TS (budgeted faithfulness) pays off. Distinct from
FP2 (:694).
