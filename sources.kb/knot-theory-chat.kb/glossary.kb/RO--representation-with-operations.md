# RO — representation-with-operations

`chat.md:429` (msg 022): search objects are triples (D, ops, ⟦·⟧)
where ⟦·⟧ must be a homomorphism — the operator's clarification that
"representation" includes the operations.
