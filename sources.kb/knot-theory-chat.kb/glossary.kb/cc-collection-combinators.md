# CC — collection combinators

`chat.md:635` (msg 032): the one genuine core seam STTT exposed —
knots needed embarrassingly-parallel map; graph traversal needs
frontier/dedup/fixpoint combinators in the core vocabulary.
