# SI — spine inversion

`chat.md:757` (msg 040): the certificate ledger moves from output
trace to central object — the claim store *is* the system. See
`../../../claims.kb/monotonic-dynamic-seam.md` context and ladder row.
