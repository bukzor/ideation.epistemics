# L4′ — revised tool verdict

`chat.md:817` (msg 044): the split falls out of SI's vocabulary —
Agda hosts the ledger kernel (ACS); Lean remains a certified-status
discharge engine (O6 opens: cross-prover certificates).
