# DP — domain plugin boundary

`chat.md:580` (msg 028): generic engine + domain plugins supplying
signatures, theorems, tables — the answer to the generality demand.
Carried into the FP2/plugin prompt architecture.
