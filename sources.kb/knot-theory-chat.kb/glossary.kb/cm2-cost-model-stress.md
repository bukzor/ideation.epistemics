# CM2 — cost-model stress

`chat.md:637` (msg 032): workload class shifts from compute-bound
GEMM to memory-bound irregular access; handled by existing design
(cost model is a declared axiom, swappable). Distinct from CM (:243).
