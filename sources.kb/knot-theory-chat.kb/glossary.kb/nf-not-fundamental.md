# NF — what doesn't survive as axiom

`chat.md:726` (msg 038): FP2 and GL's residue are theorems; cost
models and budgets are instance axioms (status three). The
method/instance split falls out free.
