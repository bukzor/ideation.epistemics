---
label: ROT_LIST
standing: open
why:
  - components.md
  - ../model.kb/effective-basis-is-computed-from-grounds-not-stored.md
---

# Is the component list exhaustive?

The five components are the owner's examples plus one accepted addition,
and the owner expects the exploration to close the list rather than a
ruling:

> [!@bukzor] f65fbdf3#L815
> That's a good open question to include in the ledger. I'm hoping the exploration can help us answer it.

Open: whether a kind of rot exists that no component counts. The
witness is a ledger the owner distrusts whose vector reads zero, and it
cannot come from the sandbox, since `ATOM_CONTENT` strips the content
that stale and near-duplicate rot live in; it is a field measurement
over the fleet's ledgers (`SANDBOX_VS_FIELD`). The question's other
half, whether zero on every component leaves every claim trusted, is
closed by `EMPTY_QUEUE_TRUST`: an empty queue entails it, and the
converse fails.
